"""YG/ZF-1: 화면 전체가 공유하는 "핵심 인자" 판정 임계값의 단일 소스.

파레토 기여율이 이 값 이상인 인자를 "핵심 인자"로 인정한다 -- 수율
예측의 신뢰도(n/5)·권장사항 분기·발송 블록 2(타깃별 TOP N), 모니터링의
FMEA 표 행 선정·권고 조치 대상, 원인 분석의 인자 카드 표시 기준이 전부
이 값을 공유한다. 이전에는 20.0이었고 일부 모듈에 "하지 말 것: 20%
임계를 낮추지 마라"는 주석이 있었다 -- 이번 작업 지시서가 명시적으로
10%로 낮추라고 요구해 그 결정을 대체한다(현재 번들 데이터셋에서는 각
타깃의 2위 인자 기여율이 4~6%대라 10%로 낮춰도 선정 결과 자체는
바뀌지 않는다 -- 다른 데이터셋에서 2·3위가 10~20% 구간에 있을 때만
차이가 난다. 폐기된 20% 결정과 그 배경은 docs/decisions.md 참고).
"""

from __future__ import annotations

CORE_FACTOR_CONTRIBUTION_MIN = 10.0
