"""화면 전체가 공유하는 "핵심 인자" 판정 임계값의 단일 소스.

파레토 기여율이 이 값 이상인 인자를 "핵심 인자"로 인정한다 -- 수율
예측의 신뢰도(n/5)·권장사항 분기·발송 블록 2(타깃별 TOP N), 모니터링의
FMEA 표 행 선정·권고 조치 대상, 원인 분석의 인자 카드 표시 기준이 전부
이 값을 공유한다. 한 곳에서만 바꿔야 여섯 화면의 "핵심 인자"가 계속
같은 집합을 가리킨다.

10%라는 값 자체는 현재 번들 데이터셋에서는 선정 결과를 바꾸지 않는다
-- 각 타깃의 2위 인자 기여율이 4~6%대라 어차피 미달이다. 2·3위가
10~20% 구간에 들어오는 다른 데이터셋에서만 차이가 난다.
"""

from __future__ import annotations

CORE_FACTOR_CONTRIBUTION_MIN = 10.0
